steps:
  # Pull last build for caching
  - name: "gcr.io/cloud-builders/docker"
    entrypoint: "bash"
    args: ["-c", "docker pull gcr.io/$PROJECT_ID/${_ENV}-${_SVC}:latest || exit 0"]
  # Build new image
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t",
        "gcr.io/$PROJECT_ID/${_ENV}-${_SVC}",
        "--cache-from",
        "gcr.io/$PROJECT_ID/${_ENV}-${_SVC}",
        "${_PATH}",
      ]
  # Push new image
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/$PROJECT_ID/${_ENV}-${_SVC}"]
  # Deploy to Cloud Run
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      [
        "run",
        "deploy",
        "--platform",
        "managed",
        "${_ENV}-${_SVC}",
        "--image",
        "gcr.io/$PROJECT_ID/${_ENV}-${_SVC}",
        "--region",
        "us-central1",
        "--update-env-vars",
        "${_EVARS}"
      ]

substitutions:
  _ENV: production
  _PATH: src/${_SVC}
  _SVC: placeholder
  _EVARS: 

images:
  - "gcr.io/$PROJECT_ID/${_ENV}-${_SVC}"